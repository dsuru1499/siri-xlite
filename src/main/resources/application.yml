siri-xlite:
  port: 8443
  estimated-vehicule-journey:
    max-age: 30
    s-maxage: 60

logging:
  level:
    root: INFO
    org.springframework.data.mongodb.core.ReactiveMongoTemplate: DEBUG
    org.mongodb: INFO

spring:
  data:
    mongodb:
      database: siri

infinispan:
  embedded:
    enabled: false
    config-xml: infinispan.xml
